[package]
name = "delayed-coding-ffi"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
publish = false

[lib]
name = "delayed_coding_ffi"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
delayed-coding = { path = ".." }

[features]
log-schedule = ["delayed-coding/log-schedule"]
