[package]
name = "dc-avx512-prototype"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
license = "MIT"
publish = false

[workspace]

[dependencies]
delayed-coding = { path = "../../..", features = ["log-schedule"] }

[profile.release]
lto = "thin"
codegen-units = 1
